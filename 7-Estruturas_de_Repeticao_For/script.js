// inicialização ; condição ; finalização

for (
  // alert("Inicializando o for")
  let index = 0; 
  index <= 10; 
  index++
  // alert("Finalizando um bloco de repetição")
) {
  alert("Indice = " + index)
}

let nome = "Igor Castilhos"

for (let i = 0; i < nome.length; i++) {
  console.log(nome[i]);
}